<%
    const loggedIn = currentUser !== undefined;
    const isManager = currentUser && currentUser.role === "Manager"
    const menu = [
        {msgId: 'home', link: '/', show: true},
        {msgId: 'flowers_catalog', link: '/flowers', show: loggedIn},
        {msgId: 'users_management', link: '/users', show: loggedIn},
        {msgId: 'branches_management', link: 'barnches', show: loggedIn && isManager},
        {msgId: 'about_company', link: 'about', show: true},
        {msgId: 'contact', link: 'contact', show: true}
    ];
    const currentLink = '/home'; // TODO! window.location.pathname
    const itemIsActive = (item) => currentLink == item.link;
%>
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Flower Fly Online Store</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>
<script>
</script>
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-left" href="#">
        <img height="65" width="65" src="/images/logo.jpg" style="">
      </a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <%menu.filter(item => item.show).forEach(item => {
            %><li class=<%=itemIsActive(item)?"active":""%>><a href="<%=item.link%>"><%=msg.find(item.msgId)%></a></li><%
        })%>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <%if (currentUser) {%>
          <h5 style="color: whitesmoke"><%=msg.find('hello_username', currentUser.fullName)%></h5>
          <li><a href="./login/logout"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
        <% } else {%>
          <li><a href="./login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div>The main body...</div>
<div>The main body...</div>
<div>The main body...</div>
<div>The main body...</div>
<div>The main body...</div>
<footer class="container-fluid text-center">
  <p>Footer Text</p>
</footer>

</body>
</html>
