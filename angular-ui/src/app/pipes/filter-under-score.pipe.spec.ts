import { FilterUnderScorePipe } from './filter-under-score.pipe';

describe('FilterUnderScorePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUnderScorePipe();
    expect(pipe).toBeTruthy();
  });
});
